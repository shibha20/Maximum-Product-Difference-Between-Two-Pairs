class Solution(object):
    def maxProductDifference(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """

        final = sorted(nums)

        print(final)

        high = final[len(final)-1]*final[len(final)-2]
        low = final[0]*final[1]
        return (high-low)
